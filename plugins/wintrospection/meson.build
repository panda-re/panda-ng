# dependencies += dependency('libosi')
# dependencies += dependency('libiohal')
# dependencies += dependency('liboffset')
# -losi -liohal -loffset

dependencies += libosi_static_dep

sources += files('wintrospection.cpp',
                 'pandamemory.cpp')